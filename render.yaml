services:
  - type: web
    name: deepfake-detect-server
    env: node
    rootDirectory: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 3000
